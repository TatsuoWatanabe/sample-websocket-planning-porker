import { App }     from './app';
import { Msgs }    from './msgs';
import { Headers } from './headers';
import { Cookies } from './cookies';

export const Consts = {
  App,
  Msgs,
  Headers,
  Cookies
};
